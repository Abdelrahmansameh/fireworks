import { GAME_BOUNDS } from '../config/config.js';

class Crowd {
    constructor(scene) {
    }

    addPerson() {
    }

    update(deltaTime) {
    }

    dispose() {
    }
}


export default Crowd;
